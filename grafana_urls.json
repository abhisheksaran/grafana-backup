{
	"grafana_urls":[
		{
			"name": "$GRAFANA_HOST_NAME",
			"url": "$GRAFANA_URL",
			"api_key": "$GRAFANA_KEY"
		}
	],
	"backup":{
		"local":{
			"backup_folder": "$BACKUP_FOLDER_NAME",
			"enabled": "$LOCAL_BACKUP"
		}
	}
}
